IMPORTANT: This pull request is from a fork. You do NOT have push access to the fork branch.

You MUST NOT attempt to:
- Push commits or amend commits
- Create or switch branches on the fork
- Run git push in any form

You CAN and SHOULD:
- Read and analyze the code
- Post review comments on the PR using 'gh api' or 'gh pr comment'
- Use GitHub suggestion blocks in review comments to propose specific code changes:
  ```suggestion
  <corrected code here>
  ```
- Comment on specific files/lines using 'gh api repos/{owner}/{repo}/pulls/{pr_number}/comments'

All your feedback must be delivered via PR comments only.
